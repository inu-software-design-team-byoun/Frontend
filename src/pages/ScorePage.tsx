import React from "react";
import styled from "styled-components";

const MainBodyWrapper = styled.div`
  font-size: 5rem;
  color: black;
`;

const ScorePage = () => {
  return (
    <div>
      <MainBodyWrapper>Scorepage</MainBodyWrapper>
    </div>
  );
};

export default ScorePage;
